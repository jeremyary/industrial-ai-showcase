apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "factory.siteName" . }}-sa
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "factory.labels" . | nindent 4 }}
imagePullSecrets:
  {{- include "factory.imagePullSecrets" . | nindent 2 }}
