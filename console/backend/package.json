{
  "name": "showcase-console-backend",
  "version": "0.1.0",
  "description": "Showcase Console API — Kafka event SSE stream, scenario control, cluster topology. Phase 1 skeletal.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "fastify": "^5.2.1",
    "kafkajs": "^2.2.4",
    "undici": "^6.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
