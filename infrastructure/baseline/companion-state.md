# Companion Cluster Baseline

Stub. Populated in Session 10 once the cluster reaches `all cluster operators Available` post-install.

- **Captured**: TBD (Session 10)
- **Cluster role**: companion (per ADR-017)
- **Host**: Fedora 43 on GMKTec Evo-X2, VM-based per OD-8 resolution (see `docs/09-risks-and-open-questions.md`)
- **Target state**: OCP 4.21.5 SNO at `api.companion.lab.local` / `10.0.0.80`

Session 10 runs the same `oc` inventory captures as `osd-hub-state.md` and documents any drift from the hub baseline.
