apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: vault-secrets-operator
  namespace: vault-secrets-operator
spec: {}  # AllNamespaces - VSO projects Secrets into tenant namespaces
