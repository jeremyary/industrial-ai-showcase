apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: cnpg-system
  namespace: cnpg-system
spec: {}  # AllNamespaces mode — CNPG manages Clusters in tenant namespaces
          # (e.g., mlflow, future fleet-manager, langgraph-orchestrator).
