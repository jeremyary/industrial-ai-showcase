apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: mes-stub
  namespace: fleet-ops
  labels:
    app: mes-stub
    phase: phase-2
spec:
  source:
    type: Git
    git:
      uri: https://github.com/jeremyary/industrial-ai-showcase.git
      ref: main
    sourceSecret:
      name: git-source-secret
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: workloads/mes-stub/container/Containerfile
  output:
    to:
      kind: ImageStreamTag
      name: mes-stub:latest
  triggers:
    - type: ConfigChange
