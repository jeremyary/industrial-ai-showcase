apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-stub
  namespace: fleet-ops
  labels:
    app: wms-stub
    phase: phase-1
  annotations:
    image.openshift.io/triggers: |-
      [{"from":{"kind":"ImageStreamTag","name":"wms-stub:latest","namespace":"fleet-ops"},"fieldPath":"spec.template.spec.containers[?(@.name==\"wms-stub\")].image"}]
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: wms-stub
  template:
    metadata:
      labels:
        app: wms-stub
    spec:
      serviceAccountName: wms-stub
      containers:
        - name: wms-stub
          image: wms-stub:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082
              name: http
          env:
            - name: SERVICE_NAME
              value: wms-stub
            - name: LOG_LEVEL
              value: INFO
            - name: ENVIRONMENT
              value: demo
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: fleet-kafka-bootstrap.fleet-ops.svc:9092
            - name: KAFKA_SECURITY_PROTOCOL
              value: PLAINTEXT
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
