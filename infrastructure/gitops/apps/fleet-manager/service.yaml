apiVersion: v1
kind: Service
metadata:
  name: fleet-manager
  namespace: fleet-ops
  labels:
    app: fleet-manager
spec:
  selector:
    app: fleet-manager
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
