apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: warehouse-data
type: Opaque
# Phase-1 placeholder creds. Vault-seeded VaultStaticSecret replacement tracked
# in the Nucleus KNOWN-ISSUES pattern — lands when any consumer outside the
# cluster needs to read from this MinIO.
stringData:
  AWS_ACCESS_KEY_ID: warehouse-camera-admin
  AWS_SECRET_ACCESS_KEY: warehouse-camera-password
