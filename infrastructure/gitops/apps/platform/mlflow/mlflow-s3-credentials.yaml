# PLACEHOLDER - same values as minio-root, exposed in AWS-SDK-expected
# key names so MLflow can pick them up via envFrom. Replaced by Vault-
# sourced ExternalSecret in Session 08.
apiVersion: v1
kind: Secret
metadata:
  name: mlflow-s3-credentials
  namespace: mlflow
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: mlflow-root
  AWS_SECRET_ACCESS_KEY: phase-0-placeholder-rotate-in-s08
