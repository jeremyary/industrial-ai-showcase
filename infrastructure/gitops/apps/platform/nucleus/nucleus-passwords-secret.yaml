# TODO(ADR-024 Part D): migrate to VaultStaticSecret reading kv/nucleus/passwords.
# Kept as a plain Secret in Part B to keep the chart reaching Ready state
# quickly. The placeholder values match the upstream chart's defaults; rotate
# before exposing Nucleus beyond the lab. See KNOWN-ISSUES.md.
apiVersion: v1
kind: Secret
metadata:
  name: nucleus-passwords
  namespace: omniverse-nucleus
  labels:
    app.kubernetes.io/part-of: omniverse-nucleus
    app.kubernetes.io/version: "2023.2.9"
type: Opaque
stringData:
  master-password: "omniverse123"
  service-password: "service123"
