apiVersion: v1
kind: Service
metadata:
  name: plc-gateway-01
  namespace: factory-floor
  labels:
    app: plc-gateway
spec:
  selector:
    app: plc-gateway
  ports:
    - name: opcua
      port: 4840
      targetPort: 4840
      protocol: TCP
