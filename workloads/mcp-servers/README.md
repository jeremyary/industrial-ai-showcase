# mcp-servers

**Role**: MCP-wrapped tool surfaces for the LangGraph agentic orchestrator (Phase 3).

**Phase**: 3. Do not populate in Phase 1.

## Planned servers

- `mcp-isaac-sim/` — wraps Isaac Sim scenario launch + query operations.
- `mcp-fleet/` — wraps Fleet Manager read + (HIL-gated) write operations.
- `mcp-mlflow/` — wraps MLflow experiment + model-registry operations.
- `mcp-nucleus/` — **deferred** unless a specific demo beat earns it. Per `demos/60-min-deep-dive/script.md` hard constraint: do not pad the agent tool surface.

## Status

Empty scaffold. Phase 3.
